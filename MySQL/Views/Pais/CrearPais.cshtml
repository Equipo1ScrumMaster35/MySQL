@model PaisModel
@{
    ViewData["Title"] = "CrearPais";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Crear País
    </div>
    <div class="card-body">
        <form asp-action="CrearPais" asp-controller="Pais" method="post">
            <div class="form-group">
                <label>Código país</label>
                <input asp-for="cod_pais" type="text" class="form-control">
                <span asp-validation-for="cod_pais" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Nombre país</label>
                <input asp-for="nom_pais" type="text" class="form-control">
                <span asp-validation-for="nom_pais" class="text-danger"></span>
            </div>
            <br />
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a asp-action="ListarPais" asp-controller="Pais" class="btn btn-warning">Volver a la lista</a>
        </form>
    </div>
</div>

